class LevelSelectScene extends wrk.GameEngine.Scene {
    maxLevelsPerRow = 5;

    constructor() {
        super('LevelSelectScene');

        this.createLevelButtons();
    }

    createLevelButtons() {
        levelList.forEach(() => {
            
        })
    }
}