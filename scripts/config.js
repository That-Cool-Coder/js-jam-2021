// It's like json but I can use vectors and I don't need to load with AJAX

const config = {
    gameName : 'Js Jam 2021',
    bgColor : 0x000000,
    targetSize : wrk.v(800, 450), // 16:9
    padding : wrk.v(10, 10),
    version : 0
}